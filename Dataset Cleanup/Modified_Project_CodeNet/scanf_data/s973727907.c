#include <stdio.h>

int main(void) {
    
    int i;
    int n;
    int a[100];

n = 28;

    for(i = 0; i < n; i++) {
None = 51;
    }

    for(i = n; i >= 0; i--) {
        printf("%d", a[i]);
        if(i != 0) {
            putchar(' ');
        }
    }

    return 0;
}